# 文件下载
[TOC]

## 文件下载

```python
def download():
    """
    docstring
    """
    # 下载文件
    for cursor in fs.find():
        print(cursor)
        File = cursor.filename
        content = cursor.read()
        with open("./mongeostore_env/upload/%s" % File, 'wb') as f:
            f.write(content)
```

